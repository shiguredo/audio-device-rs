# examples

## device_list

デバイス一覧を JSON で出力する。

```bash
cargo run --example device_list
```

## device_info

デバイスごとの詳細情報と統計情報を JSON で出力する。

```bash
cargo run --example device_info
```

## playback_sine

デフォルト出力デバイスに 440Hz のサイン波を再生する。

```bash
cargo run --example playback_sine
```
